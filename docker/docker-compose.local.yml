services:
  site:
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - ../dist:/srv/site:ro
    environment:
      - APP_ENV=local

  worker:
    environment:
      - APP_ENV=local
      - APTPLANS_FILES=/var/lib/aptplans/files
      - OLLAMA_HOST=http://ollama:11434
